import styled from 'styled-components';

const StyledCard = styled.div`
	width: 264px;
	height: 336px;
	background: #ffffff;
	box-shadow: 0px 0px 7px 2px rgba(0, 0, 0, 0.0294384);
	border-radius: 5px;
	margin-right: auto;
	margin-left: auto;
	margin-bottom: 1rem;
`;
const StyledImage = styled.img`
	width: 264px;
	height: 160px;
	overflow: hidden;
	margin-bottom: 1rem;
`;
const StyledTextCard = styled.div`
	margin-left: 1rem;
`;
export { StyledCard, StyledImage, StyledTextCard };
